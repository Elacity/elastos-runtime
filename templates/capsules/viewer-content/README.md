# Viewer And Content Pair

Copy `viewer.capsule.json` to `capsules/<viewer>/capsule.json` with the browser
directory. Copy `content.capsule.json` and `sample.example` to a separate content
capsule. Rename both sides together so the content `viewer`, viewer
`input_schema.accepts[*].viewer`, extension, and MIME declarations agree.
