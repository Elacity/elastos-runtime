{
  "schema": "elastos.capsule/v1",
  "name": "tunnel-provider",
  "version": "0.1.0",
  "description": "Carrier network provider for elastos://tunnel/ (public cloudflared quick tunnels)",
  "author": "elastos",
  "role": "provider",
  "type": "microvm",
  "entrypoint": "rootfs.ext4",
  "requires": [
    { "name": "cloudflared", "kind": "external" }
  ],
  "provides": "elastos://tunnel/*",
  "authority": {
    "reason": "Explicit operator-controlled public tunnel provider for exposing selected local services without giving app capsules raw network authority.",
    "capabilities": [
      {
        "resource": "elastos://tunnel/*",
        "actions": ["admin"],
        "operations": ["start", "stop", "status", "ping"]
      }
    ],
    "audit_events": ["tunnel.start", "tunnel.stop", "tunnel.status"]
  },
  "interfaces": [
    {
      "id": "elastos.provider.tunnel",
      "version": "0.5.0",
      "description": "Runtime provider affordance descriptors projected from tunnel-provider authority metadata; descriptors are not grants.",
      "methods": [
        {
          "id": "start",
          "description": "Request start through the tunnel-provider Runtime provider boundary for elastos://tunnel/*.",
          "risk": "privileged",
          "approval": "user",
          "audit": "full",
          "resource": "elastos://tunnel/*",
          "operation": "start"
        },
        {
          "id": "stop",
          "description": "Request stop through the tunnel-provider Runtime provider boundary for elastos://tunnel/*.",
          "risk": "privileged",
          "approval": "user",
          "audit": "full",
          "resource": "elastos://tunnel/*",
          "operation": "stop"
        },
        {
          "id": "status",
          "description": "Request status through the tunnel-provider Runtime provider boundary for elastos://tunnel/*.",
          "risk": "read",
          "approval": "runtime_policy",
          "audit": "summary",
          "resource": "elastos://tunnel/*",
          "operation": "status"
        },
        {
          "id": "ping",
          "description": "Request ping through the tunnel-provider Runtime provider boundary for elastos://tunnel/*.",
          "risk": "read",
          "approval": "runtime_policy",
          "audit": "summary",
          "resource": "elastos://tunnel/*",
          "operation": "ping"
        }
      ]
    }
  ],
  "resources": {
    "memory_mb": 64,
    "gpu": false
  },
  "permissions": {
    "host_process": true
  }
}
