[package]
name = "services"
version = "0.1.0"
edition = "2021"
description = "Services management capsule"
license = "MIT"

[dependencies]
elastos-guest = { path = "../../elastos/crates/elastos-guest" }

[profile.release]
opt-level = "s"
lto = true

[workspace]
