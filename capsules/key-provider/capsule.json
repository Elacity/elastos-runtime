{
  "schema": "elastos.capsule/v1",
  "name": "key-provider",
  "version": "0.2.0",
  "description": "Runtime key-release boundary for protected-content decrypt sessions",
  "author": "elastos",
  "role": "provider",
  "type": "microvm",
  "entrypoint": "rootfs.ext4",
  "provides": "elastos://key/*",
  "authority": {
    "reason": "Validates protected-content key-release requests and owns PQ-hybrid dKMS backend authority without exposing raw CEKs, KMS node credentials, chain RPC, or wallet RPC to app capsules.",
    "capabilities": [
      {
        "resource": "elastos://key/*",
        "actions": ["read"],
        "operations": ["status", "release"]
      }
    ],
    "audit_events": ["key.status", "key.release.denied", "key.rewrap.denied"]
  },
  "interfaces": [
    {
      "id": "elastos.provider.key",
      "version": "0.5.0",
      "description": "Runtime provider affordance descriptors projected from key-provider authority metadata; descriptors are not grants.",
      "methods": [
        {
          "id": "status",
          "description": "Request status through the key-provider Runtime provider boundary for elastos://key/*.",
          "risk": "read",
          "approval": "runtime_policy",
          "audit": "summary",
          "resource": "elastos://key/*",
          "operation": "status"
        },
        {
          "id": "release",
          "description": "Request release through the key-provider Runtime provider boundary for elastos://key/*.",
          "risk": "rights",
          "approval": "user",
          "audit": "full",
          "resource": "elastos://key/*",
          "operation": "release"
        }
      ]
    }
  ],
  "resources": {
    "memory_mb": 64,
    "gpu": false
  },
  "permissions": {}
}
