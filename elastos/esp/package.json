{
  "name": "@elastos/esp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESP v0 — ElastOS Shell Protocol shared types + read-only projection client (two-channel object, hero consent act). See docs/ESP_V0.md.",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test build/two_channel.test.js build/shell_picker.test.js build/ai_act_audit.test.js build/spend_audit.test.js capsule_custody_panel.test.mjs"
  },
  "devDependencies": {
    "svelte": "^5.56.4",
    "typescript": "^5.9.3"
  }
}
