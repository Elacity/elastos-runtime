[package]
name = "elastos-wallet-contract"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Transport-independent authority-bound Wallet Bus v2 schema"

[lib]
name = "elastos_wallet_contract"
path = "src/lib.rs"

[dependencies]
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
