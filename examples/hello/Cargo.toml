[package]
name = "hello"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wit-bindgen = "0.57.1"

[profile.release]
opt-level = "s"
lto = true

# Keep this manual example outside the main ElastOS Cargo workspace.
[workspace]
